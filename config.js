// ------------------------------------------------------------------
// Comment backend config.
//
// Leave these blank to run in "local" mode: replies save only in the
// visitor's own browser (fine for testing, but nobody else sees them).
//
// To make comments REAL and shared for everyone, create a free Supabase
// project, run the SQL from the setup notes, then paste your values here.
// The anon key is safe to expose in the browser — that's what it's for.
// ------------------------------------------------------------------
window.SITE_CONFIG = {
  supabaseUrl: "https://nzbdrsjjkuznroomqrug.supabase.co",  // e.g. "https://abcdxyz.supabase.co"
  supabaseKey: "sb_publishable_k4WbbURqAycUyn78bDQ2-w_Gpv4Usf5",  // your project's anon / public key
};
